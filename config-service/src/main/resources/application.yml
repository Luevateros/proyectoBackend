server:
  port: 8888
  
spring:
  cloud:
    config:
      server:
        git:
          uri: https://luevateros@bitbucket.org/luevateros/config-data.git
          search-paths: api
          default-label: "master"

  security:
    user: 
      name: root
      password: admin 